@startuml 歌曲播放流程

start

:用户浏览歌曲列表;
:点击播放歌曲;
:获取歌曲文件URL;
:初始化音频播放器;
:开始播放;

while (播放中？) is (是)
  if (用户操作？) then (暂停)
    :暂停播放;
  else if (切换歌曲)
    :停止当前播放;
    :播放新歌曲;
  else if (调整音量)
    :更新音量设置;
  else if (拖动进度条)
    :跳转到指定位置;
  endif
endwhile (否)
:播放结束;
:更新播放记录;

stop

@enduml