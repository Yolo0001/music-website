@startuml 歌曲上传审核流程

start

:用户上传歌曲;
:填写歌曲信息;
:提交审核;

:系统初步验证;
if (格式是否正确？) then (否)
  :退回修改;
  stop
else (是)
  :进入待审核队列;
endif

:管理员审核;
if (内容是否合规？) then (否)
  :审核不通过;
  :通知用户;
  stop
else (是)
  :审核通过;
  :生成歌曲ID;
  :存储文件;
  :更新歌曲库;
  :通知用户;
endif

:上架展示;
stop

@enduml